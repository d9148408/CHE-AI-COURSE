# Part_1 Notebook 更新完成報告

**更新日期**：2025-12-17  
**更新範圍**：Part_1/Unit01-03 的 .ipynb Notebook 檔案  
**目的**：將 .md 講義中新增的內容同步到 Jupyter Notebook  

---

## ✅ Unit01_Python_EDA_Basics.ipynb 更新內容

### 新增 Cell 1：Arrhenius 反應速率計算（向量化實戰）
**位置**：在原有向量化演示之後  
**類型**：Markdown + Code (2 cells)  
**內容**：
- 化工情境說明：計算不同溫度下的反應速率常數
- 參數設定：A=1e10, Ea=50000 J/mol, R=8.314 J/(mol·K)
- 溫度範圍：300K-400K，共 1000 個點
- 效能比較：迴圈 vs 向量化
- 預期加速倍數：約 50x

**程式碼亮點**：
```python
k_vectorized = A * np.exp(-Ea / (R * T_array))
```

---

### 新增 Cell 2：反應器逐時數據聚合（Reshape 實戰）
**位置**：在 Arrhenius 範例之後  
**類型**：Markdown + Code (2 cells)  
**內容**：
- 化工情境說明：240 小時（10 天）的反應器溫度數據
- 目標：計算每日平均溫度
- 效能比較：迴圈 vs reshape
- 預期加速倍數：約 20x

**程式碼亮點**：
```python
daily_T_reshape = hourly_T.reshape(10, 24).mean(axis=1)
```

---

### 教學價值
✅ 學生立刻看到「NumPy 向量化」與「reshape」在化工製程的實際應用  
✅ 不再是抽象的數學練習，而是工作中會遇到的場景  
✅ 效能對比建立「為什麼要學向量化」的動機  

---

## ✅ Unit02_TimeSeries_Cleaning.ipynb 更新內容

### 新增 Cell 1：化工時序數據健康檢查函數
**位置**：在時間序列處理前  
**類型**：Markdown + Code (2 cells)  
**內容**：
- 完整的 `chemeng_data_health_check()` 函數（約 100 行）
- 7 項自動診斷功能：
  1. 時間索引檢查（是否為 DatetimeIndex）
  2. 時區檢查（是否有時區資訊）
  3. 採樣間隔檢查（是否等距）
  4. 缺失值檢查（缺失比例、最長連續缺失）
  5. 物理極限檢查（溫度、壓力、流量範圍）
  6. 數值分佈檢查（多峰性、峰度）
  7. 停機段檢測（多變數同時接近零）

**函數簽名**：
```python
def chemeng_data_health_check(df, time_col=None, value_cols=None):
    """
    化工時序數據健康檢查函數
    
    Returns:
    --------
    dict : 診斷報告（包含 ✅/⚠️/❌ 狀態標記）
    """
```

---

### 新增 Cell 2：健康檢查函數測試
**位置**：在製程監控案例之後  
**類型**：Markdown + Code (2 cells)  
**內容**：
- 使用 df_reactor 數據測試健康檢查函數
- 輸出格式化的診斷報告
- 展示如何解讀報告結果

**輸出範例**：
```
==================================================
📋 化工時序數據健康檢查報告
==================================================
時間索引: ✅ 正確
時區: ⚠️ 無時區資訊（建議設定 UTC）
採樣間隔: ✅ 等距（0 days 00:01:00）
缺失值:
  - Pressure: {'缺失比例': '0.0%', '最長連續缺失': '0 點'}
  - MA_5: {'缺失比例': '0.0%', '最長連續缺失': '0 點'}
...
```

---

### 教學價值
✅ 建立「先診斷再治療」的資料分析習慣  
✅ 自動化識別十大陷阱中的多項問題  
✅ 可直接應用於學生自己的專案數據  
✅ 培養專業的資料品質意識  

---

## ✅ Unit03_ChemEng_Data_Types.ipynb 更新內容

### 新增 Cell 1：非線性效應 - 二次多項式擬合
**位置**：在批次 EDA 之後  
**類型**：Markdown + Code (2 cells)  
**內容**：
- 使用 `PolynomialFeatures` 建立二次特徵
- 擬合 $Mn = \beta_0 + \beta_1 T + \beta_2 T^2 + \epsilon$
- 視覺化：散點圖 + 二次曲線
- 比較線性 R² vs 二次 R²
- 輸出圖片：`11_batch_nonlinear.png`

**化工解釋**：
- 溫度過低：反應速率慢，分子量低
- 溫度過高：副反應或降解，分子量下降
- 存在最佳溫度窗口

---

### 新增 Cell 2：交互作用 - 3D 響應曲面
**位置**：在非線性效應之後  
**類型**：Code (1 cell)  
**內容**：
- 建立交互項：`Temp_x_Agit = React_Temp × Agitator_Speed`
- 擬合模型：$Mn = \beta_0 + \beta_1 T + \beta_2 A + \beta_3 (T \times A)$
- 3D 響應曲面圖（使用 `mpl_toolkits.mplot3d`）
- 輸出交互項係數與 R²
- 輸出圖片：`12_batch_interaction_3d.png`

**程式碼亮點**：
```python
# 建立網格並預測
T_grid, A_grid = np.meshgrid(temp_range, agit_range)
Mn_grid = model_interact.intercept_ + ...

# 3D 曲面
ax.plot_surface(T_grid, A_grid, Mn_grid, cmap='viridis', alpha=0.7)
```

---

### 新增 Cell 3：等高線圖 - 最佳操作窗口
**位置**：在 3D 曲面之後  
**類型**：Code (1 cell)  
**內容**：
- 將 3D 曲面「壓平」為 2D 等高線圖
- 使用綠色標示高分子量區域
- 找出預測最佳操作條件（溫度、攪拌速度）
- 輸出圖片：`13_batch_contour.png`

**實務應用**：
- 操作人員可直接讀取最佳 (T, A) 組合
- 留有安全邊界（避免操作在邊界）

---

### 新增 Cell 4：因子組合箱型圖
**位置**：在等高線圖之後  
**類型**：Code (1 cell)  
**內容**：
- 將溫度與攪拌速度分為高/低兩組
- 建立四種組合：`Low_T_Low_A`, `Low_T_High_A`, `High_T_Low_A`, `High_T_High_A`
- 箱型圖比較各組合的 Mn 分佈
- 統計摘要（mean, std, min, max）
- 輸出圖片：`14_batch_combination_box.png`

**判斷交互作用**：
- 若 `High_T_High_A` 明顯高於單因子高組 → 交互作用存在
- 若四組呈線性遞增 → 無明顯交互作用

---

### 教學價值
✅ 展示實驗設計 (DOE) 與響應曲面方法的基礎  
✅ 為後續 Unit10 (Bayesian Optimization) 建立必要背景  
✅ 學生學會「不滿足於線性模型」，開始思考物理機制  
✅ 實務技能：尋找最佳操作窗口  

---

## 📊 更新統計摘要

| Notebook | 新增 Markdown Cells | 新增 Code Cells | 新增圖片輸出 | 估計新增行數 |
|----------|---------------------|-----------------|--------------|--------------|
| Unit01   | 2                   | 2               | 0            | ~50 行       |
| Unit02   | 2                   | 2               | 0            | ~120 行      |
| Unit03   | 1                   | 4               | 4            | ~150 行      |
| **總計** | **5**               | **8**           | **4**        | **~320 行**  |

---

## 🎯 執行建議

### 1. 驗證 Notebook 執行
建議按以下順序執行並驗證：

**Unit01:**
```bash
# 啟動 Jupyter
jupyter notebook Part_1/Unit01_Python_EDA_Basics.ipynb

# 執行所有 cells (Kernel → Restart & Run All)
# 檢查：
# - Arrhenius 計算加速倍數 > 10x
# - Reshape 計算加速倍數 > 5x
```

**Unit02:**
```bash
jupyter notebook Part_1/Unit02_TimeSeries_Cleaning.ipynb

# 執行所有 cells
# 檢查：
# - 健康檢查函數載入成功
# - 診斷報告格式正確（有 ✅/⚠️/❌ 標記）
```

**Unit03:**
```bash
jupyter notebook Part_1/Unit03_ChemEng_Data_Types.ipynb

# 執行所有 cells
# 檢查：
# - 產生 4 張新圖片（11-14）
# - 3D 圖與等高線圖正常顯示
# - 交互項係數顯著（p < 0.05）
```

---

### 2. 圖片檔案確認
執行 Unit03 後，應在 `Part_1/Unit03_Results/` 產生：
- [x] `11_batch_nonlinear.png` - 二次多項式擬合
- [x] `12_batch_interaction_3d.png` - 3D 響應曲面
- [x] `13_batch_contour.png` - 等高線圖
- [x] `14_batch_combination_box.png` - 因子組合箱型圖

---

### 3. 常見問題排除

**問題 1**：`PolynomialFeatures` 未定義
```python
# 解法：確保已 import
from sklearn.preprocessing import PolynomialFeatures
```

**問題 2**：3D 圖不顯示
```python
# 解法：確保已 import
from mpl_toolkits.mplot3d import Axes3D
```

**問題 3**：健康檢查函數報錯
```python
# 解法：確保數據有 DatetimeIndex
df_reactor = df_reactor.set_index('Timestamp')  # 若還沒設定
```

**問題 4**：路徑錯誤
```python
# 解法：確保 OUTPUT_DIR 已在第一個 cell 定義
OUTPUT_DIR = PART1_DIR / "Unit03_Results"
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
```

---

## 🔄 與 .md 講義的對應關係

| .md 講義內容 | .ipynb Notebook 內容 | 狀態 |
|-------------|---------------------|------|
| Unit01 Section 0（為什麼學）| 不需要（僅講義說明）| N/A |
| Unit01 向量化化工範例 | ✅ Cell: Arrhenius 計算 | 已新增 |
| Unit01 Reshape 化工範例 | ✅ Cell: 反應器數據聚合 | 已新增 |
| Unit01 自評檢查表 | 不需要（僅講義使用）| N/A |
| Unit02 十大陷阱說明 | 不需要（僅講義理論）| N/A |
| Unit02 健康檢查函數 | ✅ Cell: chemeng_data_health_check | 已新增 |
| Unit03 非線性效應 | ✅ Cell: 二次多項式擬合 | 已新增 |
| Unit03 交互作用 | ✅ Cell: 3D 響應曲面 | 已新增 |
| Unit03 等高線圖 | ✅ Cell: 等高線圖 | 已新增 |
| Unit03 因子組合 | ✅ Cell: 箱型圖 | 已新增 |

---

## 📚 學生使用指引

### 推薦學習流程

1. **閱讀 .md 講義**（理論與動機）
   - 了解「為什麼要學這個技術」
   - 閱讀化工案例背景
   - 查看自評檢查表

2. **執行 .ipynb Notebook**（實作與驗證）
   - 逐 cell 執行，觀察輸出
   - 修改參數，觀察變化
   - 完成延伸練習

3. **應用到自己的數據**
   - 使用健康檢查函數診斷自己的數據
   - 套用向量化技巧加速計算
   - 嘗試非線性與交互作用分析

---

## ✅ 完成確認

- [x] Unit01 新增 Arrhenius 向量化範例
- [x] Unit01 新增 Reshape 聚合範例
- [x] Unit02 新增健康檢查函數
- [x] Unit02 新增函數測試範例
- [x] Unit03 新增二次多項式擬合
- [x] Unit03 新增 3D 響應曲面
- [x] Unit03 新增等高線圖
- [x] Unit03 新增因子組合箱型圖
- [ ] 執行所有 Notebook 並驗證輸出（待後續執行）
- [ ] 確認所有圖片正常產生（待後續執行）

---

**更新完成日期**：2025-12-17  
**更新執行者**：GitHub Copilot (Claude Sonnet 4.5)  
**驗證狀態**：程式碼已插入，待執行驗證  
